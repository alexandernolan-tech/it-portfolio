# Windows Services Basics

- Services are background processes that manage features (audio, networking, printing)
- `services.msc` shows them in GUI form
- `Get-Service` lists them in PowerShell
- Many tech issues come from stopped or failed services

